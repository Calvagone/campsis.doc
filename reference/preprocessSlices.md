# Preprocess 'slices' argument.

Preprocess 'slices' argument.

## Usage

``` r
preprocessSlices(slices, maxID)
```

## Arguments

- slices:

  slices argument corresponding to the number of subjects simulated at
  once

## Value

slices if not NULL, otherwise total number of subjects
