# Factor scenarios columns if not done yet.

Factor scenarios columns if not done yet.

## Usage

``` r
factorScenarios(x, scenarios = NULL)
```

## Arguments

- x:

  data frame

- scenarios:

  scenarios
