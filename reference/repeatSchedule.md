# Repeat schedule.

Repeat schedule.

## Usage

``` r
repeatSchedule(x, schedule)

# S4 method for class 'numeric,cyclic_schedule'
repeatSchedule(x, schedule)

# S4 method for class 'numeric,repeat_at_schedule'
repeatSchedule(x, schedule)

# S4 method for class 'numeric,undefined_schedule'
repeatSchedule(x, schedule)
```

## Arguments

- x:

  object to repeat the schedule

- schedule:

  initial times vector

## Value

resulting times vector
