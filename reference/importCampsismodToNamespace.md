# Import the whole campsismod package into NAMESPACE when parsed by 'roxygen'.

Import the whole campsismod package into NAMESPACE when parsed by
'roxygen'.

## Usage

``` r
importCampsismodToNamespace()
```

## Value

always TRUE
