# Open JSON file.

Open JSON file.

## Usage

``` r
openJSON(json, schema = NULL)
```

## Arguments

- json:

  JSON in its string form or path to JSON file

- schema:

  JSON schema

## Value

parsed JSON object
