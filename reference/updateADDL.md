# Update the number of additional doses (ADDL).

Update the number of additional doses (ADDL).

## Usage

``` r
updateADDL(object, addl, ref = NULL)

# S4 method for class 'bolus_wrapper,integer,character'
updateADDL(object, addl, ref = NULL)

# S4 method for class 'infusion_wrapper,integer,character'
updateADDL(object, addl, ref = NULL)

# S4 method for class 'bolus,integer,character'
updateADDL(object, addl, ref = NULL)

# S4 method for class 'infusion,integer,character'
updateADDL(object, addl, ref = NULL)

# S4 method for class 'treatment,integer,character'
updateADDL(object, addl, ref = NULL)

# S4 method for class 'arm,integer,character'
updateADDL(object, addl, ref = NULL)

# S4 method for class 'arms,integer,character'
updateADDL(object, addl, ref = NULL)

# S4 method for class 'dataset,integer,character'
updateADDL(object, addl, ref = NULL)
```

## Arguments

- object:

  generic object

- addl:

  new number of additional doses

- ref:

  reference treatment name

## Value

updated object
