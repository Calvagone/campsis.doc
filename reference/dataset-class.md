# Dataset class.

Dataset class.

## Slots

- `arms`:

  a list of treatment arms

- `config`:

  dataset configuration for export

- `iiv`:

  data frame containing the inter-individual variability (all ETAS) for
  the export
