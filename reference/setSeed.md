# Set the seed. The goal of this method is to centralize all calls to the R method 'set.seed'.

Set the seed. The goal of this method is to centralize all calls to the
R method 'set.seed'.

## Usage

``` r
setSeed(seed)
```

## Arguments

- seed:

  seed value, not NULL
